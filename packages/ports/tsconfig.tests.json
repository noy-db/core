{
  "//": "Typechecks the non-capsule test files against @noy-db/hub's BUILT types, one nominal identity per shared type. The capsule tests need hub SOURCE (see tsconfig.tests.capsule.json) and mixing the two programs gives every shared type two identities \u2014 measured 2026-09-21 on __tests__/as/fixtures/format.ts. Two programs, on purpose. `types` is [] and never defaulted (family#28 trap).",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "rootDir": ".",
    "noEmit": true,
    "types": [],
    "noImplicitAny": false
  },
  "include": [
    "src",
    "__tests__"
  ],
  "exclude": [
    "__tests__/capsule"
  ]
}
