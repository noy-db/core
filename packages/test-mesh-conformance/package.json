{
  "name": "@noy-db/test-mesh-conformance",
  "version": "0.9.0-pre.1",
  "description": "Parameterized contract tests for noy-db by-* transports — the conformance suite every NoydbMesh implementation must pass — now a re-export of @noy-db/ports/by; deprecated in favour of it",
  "license": "Apache-2.0",
  "author": "vLannaAi <vicio@lanna.ai>",
  "homepage": "https://github.com/noy-db/core/tree/main/packages/test-mesh-conformance#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/noy-db/core.git",
    "directory": "packages/test-mesh-conformance"
  },
  "bugs": {
    "url": "https://github.com/noy-db/core/issues"
  },
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "README.md",
    "LICENSE",
    "NOTICE"
  ],
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.tests.json"
  },
  "peerDependencies": {
    "@noy-db/hub": "workspace:^",
    "vitest": "^3.0.0"
  },
  "devDependencies": {
    "@noy-db/hub": "workspace:*",
    "vitest": "^3.0.0"
  },
  "keywords": [
    "noy-db",
    "conformance",
    "coordination",
    "transport",
    "testing"
  ],
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@noy-db/ports": "workspace:^"
  }
}
